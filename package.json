{
  "name": "opencode-worklog",
  "version": "0.1.0",
  "description": "Cross-session worklog system for OpenCode — todos, blockers, decisions, ADRs, and session checkpoints.",
  "type": "module",
  "main": "index.ts",
  "exports": {
    ".": "./index.ts"
  },
  "keywords": ["opencode", "opencode-plugin", "worklog", "adr", "todos", "session"],
  "license": "MIT",
  "peerDependencies": {
    "@opencode-ai/plugin": "*"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "latest"
  }
}
